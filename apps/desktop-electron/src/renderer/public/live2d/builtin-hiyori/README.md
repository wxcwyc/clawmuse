This directory contains the builtin `Hiyori` sample model assets used by the
desktop renderer.

Expected entry file:

- `Hiyori.model3.json`

These files are included so ClawMuse can run out of the box with a visible
builtin Live2D character.

They are not owned by ClawMuse and are not relicensed under this repository's
MIT license. See `NOTICE.md` in this directory and `THIRD_PARTY_NOTICES.md` at
the repository root before redistributing them.
