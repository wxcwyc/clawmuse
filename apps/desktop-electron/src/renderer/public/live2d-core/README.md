Place a licensed Live2D Cubism Core runtime file here as:

`live2dcubismcore.min.js`

The renderer HTML preloads `/live2d-core/live2dcubismcore.min.js` before the Vue app boots.

Do not copy AIRI-distributed Cubism files into this repository unless their license permits the intended use.
